# Install with:  pip install -r requirements.txt
#
# requests handles every HTTP source. openpyxl is needed only by the EMA puller:
# EMA publishes its medicines report as .xlsx with no CSV equivalent. Everything
# else (sqlite3, csv, zipfile, hashlib, re, datetime) is standard library.
requests>=2.32
openpyxl>=3.1
